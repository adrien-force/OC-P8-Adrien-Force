{% extends 'base.html.twig' %}
{% block body %}
    <!DOCTYPE html>
    <html lang="fr">
    <body>
    <div id="main-container">
        {{ include('partials/nav.html.twig') }}
        <main>
            <header>
                <div id="current-user"></div>
                <div id="title-page">
                    <div class="flex-header">
                        <h1> {{ project.name }}</h1>
                        <div class="user-list">
                            {% for employe in project.employes %}
                            <div class="avatar"> {{ employe.initials }}</div>
                            {% endfor %}
                        </div>
                        <a class="button" href=" {{ path('app_project_edit', {id: project.id}) }}">Modifier</a>
                    </div>
                </div>
            </header>
            <div id="content">
                <div id="tasks-container">
                    {% for stati in status %}
                        <div class="task-column">
                            <h2>{{ stati.name }}</h2>
                            <div class="tasks-list">
                                {% for task in project.tasks %}
                                    {% if task.status.id == stati.id %}
                                    <a class="task" href="{{ path('app_project_task_edit', {id: task.id}) }}">
                                        <h3>{{ task.title }}</h3>
                                        <p>{{ task.description }}</p>
                                        <div class="task-infos">
                                            <div class="task-deadline">
                                                <i class="fa-regular fa-calendar"></i> {{ task.deadline|date('d/m/Y') }}
                                            </div>
                                            {% if task.employe %}
                                            <div class="mini-user">
                                                {{ task.employe.lastname}}
                                            </div>
                                            {% else %}
                                            <i class="fa fa-xmark"></i>
                                            {% endif %}
                                        </div>
                                    </a>
                                    {% endif %}
                                {% endfor %}
                                <a href="{{ path('app_project_task_add', {id: project.id}) }}" class="task-add">
                                    + Ajouter une tâche
                                </a>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </main>
    </div>
    </body>
    </html>
{% endblock %}
